# EPIC-260077: Resizable Left Rail — drag-to-widen the Explorer panel

- Epic ID: EPIC-260077
- Title: Resizable Left Rail — drag-to-widen the Explorer panel
- Persona/Actor: Any user browsing or organizing taxonomy/labels in the left-rail Explorer,
  especially the Categories lens
- Value statement: For a user classifying data with Categories, deliver a resizable left rail so
  deep or wide category trees are readable without truncation, wrapping, or horizontal scrolling.
- Context: The left rail (`LeftRail.tsx`) hosts the Labels/Categories navigator lenses at a fixed
  `240px` width. EPIC-260076 added dimension nesting on top of value nesting, so `CategoryTree`'s
  indentation (`indentOption`, 4 non-breaking spaces per depth level) now compounds two levels of
  hierarchy in the same fixed-width column — display names truncate or wrap sooner than before.
- Problem statement: Users have no control over the rail/canvas width tradeoff. A fixed `240px`
  rail that was adequate for a flat label list is cramped for a nested category tree, and there is
  no workaround short of a code change.
- Expected outcome: A user can drag the rail's right edge to widen (or narrow) it within sane
  bounds, and the chosen width persists across sessions the same way the existing collapse state
  does.
- Success metric(s): unit tests cover the resize/clamp/persistence logic; e2e coverage exercises a
  drag-resize and confirms the width survives a reload; qualitative — a deep category tree is
  legible at a user-chosen width without truncation workarounds.
- Non-goals: resolved by ideation (2026-08-27) — the right rail (`Inspector.tsx`) is out of scope
  (D1), per-lens widths are out of scope (D6), and touch/narrow-viewport-specific resize
  affordances remain out of scope, the `60vw` cap (D2) standing in for a narrow-viewport story
  rather than a mobile breakpoint this app does not yet have.
- Constraints: Must preserve existing collapse behavior (`leftRailCollapsed`) unchanged, and must
  not regress the shell's existing accessibility bar (ADR-260031: keyboard operability, visible
  focus indicators).
- Dependencies: EPIC-260066 (shell foundation — `LeftRail`, slot registries, `WorkspaceContext`),
  EPIC-260068 / EPIC-260076 (Categories navigator and its dimension tree — the direct motivator).
- Risks/assumptions: Assumes the existing `localStorage`-backed preferences pattern
  (`use-preferences.ts`) is the right persistence mechanism to extend, rather than a new mechanism.
- Target window: delivered 2026-08-27; closed to Done 2026-08-29.
- Validation approach: e2e drag simulation (pointer down/move/up asserting width change and
  persistence across reload) + keyboard-resize assertion if D2 below resolves to "yes"; unit tests
  on the width-clamp function in isolation.

## Ideation Bullets — Resolved (2026-08-27, plan review; approved by the project owner this date)

- [x] **Scope: left rail only.** Right rail (`Inspector.tsx`) is out of scope for this epic — the
  reported problem (Categories tree readability) is specific to the left rail, and generalizing to
  both rails without a validated need would double the surface area for no expressed benefit. The
  resize mechanism built here is expected to be reusable if the right rail needs it later.
- [x] **Width bounds: default `240px` (unchanged), min `200px`, max `480px` further capped at 60%
  of viewport width.** Below `200px` the lens switcher tabs and `CategoryNodeEditor`'s
  `width: '100%'` inline inputs get uncomfortably cramped. The `480px` ceiling is paired with a
  `60vw` cap (`min(480, 0.6 * viewportWidth)`) so the rail can't crush the center canvas on a
  narrow browser window, without building a separate narrow-viewport feature.
- [x] **Persistence: extend `use-preferences.ts`.** New `leftRailWidth: number` /
  `setLeftRailWidth` alongside the existing `leftRailCollapsed`, same localStorage
  read/write/try-catch shape and sticky-default caveat every other preference in that file
  already has. No server-side persistence — this is a cosmetic client-local layout preference, not
  a contract change.
- [x] **Keyboard accessibility: included in v1.** The resize handle is a WAI-ARIA `separator`
  (`role="separator"`, `aria-orientation="vertical"`, `aria-valuenow`/`min`/`max`, focusable):
  ArrowLeft/ArrowRight nudge the width by a fixed step, Home/End jump to min/max. Every other
  interactive control in this shell (`GraphViewTabs`, the lens switcher in `LeftRail` itself,
  `CategoryTree`'s tree role) is keyboard-operable — a mouse-only handle would be the first
  inconsistency of its kind, and reuses the same clamp function the pointer-drag path needs anyway.
- [x] **Reset affordance: double-click the handle restores the default `240px`.** Cheap, and the
  conventional pattern for this control (VS Code, browser devtools split panes).
- [x] **Granularity: one shared width for the whole rail, not per-lens.** A per-lens width would
  make the rail visibly jump when switching Labels ↔ Categories with no expressed need for it —
  the over-engineering trap this project's other ADRs have explicitly avoided (e.g. ADR-260039
  rejected building both LR/TB Flow orientations upfront for the same reason).
- [x] **Collapse interaction: independent state, no special-casing.** Collapsing hides the rail;
  expanding restores whatever width was last set (not the default). `leftRailCollapsed` and the
  new `leftRailWidth` are two independent preferences with no dependency between them, matching
  how `use-preferences.ts` already treats every other preference pair.
- [x] **Full code + test coverage task** — included (T4).

Given the keyboard-accessibility bullet's resolution, this epic ships its own T1 ADR (matching
every other shell-capability epic's precedent: ADR-260061, ADR-260066, ADR-260070, …) documenting
the bounds, persistence key, keyboard grammar, reset affordance, and single-width decision.

## Tasks

Inline checklist — no per-task files. Task state: `To Do → In Review → Done`.

| Task | State | Commit/PR/Review |
| --- | --- | --- |
| T1 · Write ADR-260073 (left-rail resize decision: bounds, persistence key, keyboard grammar, reset, single-width) | Done | ADR-260073 (Accepted), indexed in adr-index.md |
| T2 · `use-preferences.ts`: `leftRailWidth`/`setLeftRailWidth` + `clampLeftRailWidth` (fixed bounds + 60vw cap), default `240px`, localStorage-persisted | Done | `clampLeftRailWidth` exported as the single clamp path for read/drag/keyboard/reset; `leftRailWidth` follows the exact read/write/try-catch shape of the existing preferences |
| T3 · `LeftRail.tsx`: drag handle (pointer resize), WAI-ARIA `separator` keyboard resize (arrows/Home/End), double-click reset | Done | Pointer drag via window-level `pointermove`/`pointerup` listeners; `role="separator"` with live `aria-valuenow`/`min`/`max`; found and fixed a hit-testing bug during e2e verification — the handle initially straddled the aside's edge (`right: -3`) and the aside's `overflow: auto` silently clipped half its hit area, so real pointer clicks never reached it; moved fully inside the box (`right: 0`) |
| T4 · Full code + test coverage for this epic (unit: clamp fn, preference persistence, keyboard resize, reset; e2e: pointer drag-resize + reload persistence) | Done | 1911 unit (+16: 5 clamp-fn, 4 preference persistence/clamp, 7 LeftRail handle behavior) / 92 e2e (+3: real-browser pointer drag + reload persistence, keyboard steps + double-click reset, Home-to-minimum) — all green; typecheck and lint clean |

Per-task human review (epic-process.md implementation gate) completed by the project owner;
T1–T4 accepted and this epic closed to **Done on 2026-08-29**.

Closure verification (run 2026-08-29): **1922 unit tests across 117 files, 92 e2e** — all green,
with typecheck, lint, and production build clean. Note this corrects the `1926 unit tests` figure
previously recorded in `epic-index.md`; 1922 is the count the tree actually produces at commit
`95ca5cf`, and no unit test was added or removed between that commit and this verification.

## Links (durable records spawned)

- Requirements: n/a — the resize/persistence contract is client-local shell state, not a
  requirement-taxonomy-worthy external contract; captured in the ADR instead.
- ADRs: ADR-260073 (T1); ADR-260074 (post-implementation-review follow-up — closes ADR-260073's
  accepted "not reactively re-clamped on bare window resize" risk with a zero-JS CSS cap).
- ICRs: n/a — no interface/schema change; client-local shell state only.
- Contracts: n/a.

## Learnings (post-implementation review, 2026-08-27)

Per-task code review (epic-process.md gate) on T2–T4 surfaced 15 findings before Done; 13 were
fixed directly (drag pointer-capture/stuck-drag defense, dead-zone in drag/keyboard math, focus
lost after drag, right-click starting a drag, unclamped fallback width, `aria-valuemax` ignoring
the viewport cap, per-pixel localStorage writes during drag, resize-handle/scrollbar hit-area
overlap, a redundant clipping `overflow: auto` ancestor, a state-based highlight inconsistent
with the file's own focus/blur idiom, hardcoded bounds literals in tests, and an empty-string
localStorage value silently parsing as a valid `0`). Two were deliberately not fixed inline:

- The 60vw-cap-not-reactive-on-bare-resize finding directly named a risk ADR-260073 already
  documented as an accepted, deliberate tradeoff — fixing it required a governance decision, not
  a silent code change. Resolved via ADR-260074 (zero-JS CSS `maxWidth` mitigation) after explicit
  owner approval.
- The reviewer's "5th duplicated preference read/write pattern" finding was accurate but out of
  this epic's own scope (D1: left-rail only) and touches four already-Done epics' code. Left
  unfixed here; spun into a dedicated follow-up epic instead (matching the BI-260065 precedent of
  extracting shared primitives as their own epic rather than a drive-by refactor) — **EPIC-260078**
  (Shared Persisted-Preference Helper), currently in Ideation.

### Carried forward at closure (2026-08-29)

- **EPIC-260078 is the open debt from this epic.** It exists *because* of this work: the review
  found 3 real bugs in the newest hand-copy of the `use-preferences.ts` localStorage triplet. This
  epic closing does not close that finding.
- **The reset affordance is double-click only (D5).** No keyboard equivalent was specified for
  reset, even though every other interaction here got one (D4). Not a defect against the agreed
  scope, but it is the one asymmetry in the keyboard grammar and worth revisiting if a
  keyboard-only user reports it.
- **The e2e hit-testing bug (T3) is the reusable lesson.** The handle straddled the aside's edge
  and `overflow: auto` silently clipped half its hit area — unit tests passed throughout, because
  the geometry only exists in a real browser. Shell-chrome controls positioned near a scrolling
  ancestor's boundary need real-browser coverage, not jsdom assertions.
