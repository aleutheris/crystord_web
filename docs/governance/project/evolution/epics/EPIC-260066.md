# Epic: EPIC-260066 — Foundation: App Shell, Slot Registries & Shared Widgets

- Epic ID: EPIC-260066
- Title: Foundation — App Shell, Slot Registries & Shared Widgets
- Persona/Actor: All Crystord users (indirectly); the team building every later UI capability.
- Value statement: For the product team, deliver a stable four-region UI shell with extension-point registries and a shared widget kit so that every later capability ships as an independent, non-blocking leaf.
- Context: The deployed backend (schema 9.2.0, pinned `backendSchemaRange: "~9.2.0"`) is far ahead of the UI, which still exposes only the old atoms+labels+bonds model — roughly 30% of the product. Exposing the rest (categories, compute, collaboration, account) needs a structural spine, not more ad-hoc panels.
- Problem statement: Today's `WorkspaceShell` wires features ad hoc — a conditional left result list, a single untabbed `DetailPanel`, two hardcoded canvas tabs. Without registries + shared-state contracts + shared widgets, each new feature would restructure the shell and collide with others, so independent delivery is impossible.
- Expected outcome: A shell where Top bar / Left rail / Center view-host / Right inspector each have one job and expose a registry (navigators · views · inspector tabs); shared-state contracts (working set, selection, preferences-with-defaults) owned by the shell; a shared widget kit (filter builder, permission-aware category tree, label chip editor); and a node-extension seam on `AtomNode`. Ships working on today's content (a trivial Labels navigator, the existing Flow/Network views, a basic Details inspector tab) with no new feature logic beyond the shared widgets.
- Success metric(s): Every later leaf (EPIC-260067…260075) adds a navigator/view/inspector-tab by registration only, importing no other feature (enforced by `src/architecture.test.ts`); the shell refactor keeps all existing E2E green.
- Non-goals: No new end-user feature behavior (categories UI, compute UI, account dropdown, etc. are leaves); no charts/analytics libraries (REQ-CR-260010); no real-time-collaboration engine (REQ-CR-260010); no SSR / module-federation.
- Constraints: Must extend, not fight, the enforced architecture — cross-feature import ban, `ui-shell`→barrel-only imports, adapter boundary (REQ-FR-260021), dual-view shared selection (ADR-260032 / REQ-FR-260034); branding via `branding.md` (REQ-CR-260011); CSR-first single-app.
- Dependencies: None — this is the root; all other epics depend on it.
- Risks/assumptions: Refactoring the shell risks regressing existing graph/search/detail behavior — mitigate by treating the existing E2E suite as the contract. Assumes the three registries + shared-state contracts are the right decomposition (already matches the enforced architecture).
- Target window: TBD. (Epic status lives only in `epic-index.md`, per the single-source-of-truth rule.)
- Validation approach: Existing E2E (sign-in, graph-workspace, search, create-atom) stay green; new architecture tests assert the registries exist and that features register through them with no cross-feature imports.

## Product context this epic carries (self-contained — the source brainstorm file is retired)

**Headline.** Backend pinned to schema 9.2.0 (`backendSchemaRange: "~9.2.0"`); UI shows ~30%. The written user guide (`docs/user-guide.md`) already documents categories + compute and tracks the live schema — the laggard is the **UI**, not the docs. (The guide's own pinned version is unstable — the bundle ships a stale copy and reinstalls revert hand-patches — so this brief deliberately doesn't restate it.)

**The five verbs** (each needs a home): **Discover** (`list_labels`/`listLabels`, `retrieveCategoryBrowse`, `retrieve`), **Classify** (labels + hierarchical categories: dimensions→values), **Relate** (bonds — the graph, done), **Compute** (`operation`: SUM/MINUS/PRODUCT/DIVIDE/COLLECT + dependency graph), **Collaborate/Audit** (workspaces, sharing/grants, change history).

**Substrate vision (standing direction).** The graph + extensible compute engine is a shared core; other apps (documentation, notes) are *configurations* of it, each needing its own concrete front door rather than "graph+flow" as a universal UI — the Blender lesson: the node graph is the engine, the viewport is home. Operations are open/extensible (`discoverOperations`, string-named `operation`, JSON `content`), so e.g. a "document" could later be a CONCAT/compose operation with no contract change. **Analyst is v1; the hybrid is the destination.**

**The stable shell (what this epic builds).** Four regions, each one job, each a registry slot:
- **Top bar** — context + query/facet chips · account menu (query-state; settings routes). Today: `<header>` (brand · `SearchBar` · `ThemeToggle` · Account · Sign Out) + `QuerySummary`.
- **Left rail** (collapsible) — navigate the *index*; registry of **navigators** (Labels, Categories, …). Today: `SearchResultPanel` (flat list — to be demoted).
- **Center** (view-host) — represent the *working set*; registry of **views**: **Flow** (dependencies/compute) · **Network** (relationships) · **Table** · **Board**. The node-graph (Flow/Network) is home. Today: `GraphViewTabs` switching `GraphCanvas` (= Flow view) / `NetworkCanvas`.
- **Right rail** (collapsible) — inspect/edit the *selection*; registry of **inspector tabs**: Details · Classify · Compute · History · Share. Today: `DetailPanel` (single, untabbed).

**Decoupling glue — three shared-state contracts, shell-owned:** working set (filters → atoms in scope; extends `useSearch`), selection (`selectedAtomId`, already lifted, fed to every view), preferences-with-defaults (a new localStorage-backed store alongside `feature-flags.ts`, modeled on `ThemeProvider`; home of the Q3 emphasis switch's default). No feature imports another feature.

**Shared widget kit (built once here):** the filter builder (labels + category facets — search + category browser + COLLECT), the permission-aware category tree (left-rail navigator + atom "pick" mode), the label chip editor. The atom-reference picker is single-consumer and lives in EPIC-260069. The **node-extension seam** on `AtomNode` lets compute badges (EPIC-260069) and LOD/Blender blocks (EPIC-260072) layer without colliding.

**Dependency shape:** this Foundation is the root; feature epics are mutually independent leaves — each touches slots and shared contracts, never each other.

**Standing decisions (resolved).**
- **Q1** — Primary user is the graph/knowledge person; the node-graph (Flow/Network layouts) is home; Table is a strong first-class peer, not home.
- **Q2** — Everybody authors taxonomy inline (no admin-only surface); the category tree carries edit affordances everywhere; taxonomy will later gain ownership/permissions mirroring atom sharing (planned, not built) — build the tree against that permission model from the start.
- **Q3** — Computed atoms are a core daily feature; compute-as-differentiator (analyst v1), realized with the current stack (Flow, status badges, "Explain this value", Table) — no charting library. Keep emphasis (Network-vs-Flow prominence, always-on vs on-demand badges) a configurable switch whose UI home is the EPIC-260070 (Account & Settings) Preferences pane.

**Standing constraints (from the enforced architecture).**
- Charts/analytics libs (d3, chart.js, recharts, nivo, plotly, victory) are excluded by REQ-CR-260010; adding charts later requires a deliberate ICR. Compute (EPIC-260069) ships without them.
- Sharing is **async** (grants/roles + change history), not live multiplayer — RTC engines excluded by REQ-CR-260010.
- Branding: desaturated brand palette, never color alone (REQ-CR-260011 / `branding.md`).

## Ideation Bullets

All resolved (approved 2026-06-30). Resolutions are the foundation of the task list below.

- [x] **Registry mechanism** → **static typed slot tables in `ui-shell`** (one per slot: navigators / views / inspector tabs), each entry a typed descriptor `{ id, label, when?, Component }`; the shell renders the active one. **Not** a runtime plugin registry (that would invert the `ui-shell → feature barrels` direction and is the "plugin runtime system" REQ-CR-260010 forbids). Generalizes the existing `GraphViewTabs` / `activeView` pattern. Ordering = array order; default/active selection = shell state.
- [x] **Shared-state contracts** → a shell-owned **`WorkspaceContext`** exposing `{ workingSet, selection, preferences }`, consumed by slot components; migrate `selectedAtomId` from props to context (and update the ADR-260032 architecture assertion accordingly). Working set is backed by the existing `useSearch`/`useGraphData`. **Preferences** are localStorage-backed (aligned with `ThemeProvider`), keys `homeEmphasis` and `computeBadges`, **defaults `compute` / `always`** (Q3 lean); this epic ships the contract + defaults (T4), EPIC-260070 adds the toggle UI, EPIC-260069 reads the default.
- [x] **Collapsible rails** → both rails collapsible, collapsed state persisted in preferences; left rail default **expanded** (discoverability); right inspector appears on selection (current behavior) and is collapsible.
- [x] **Node-extension seam** → a layered `AtomNode` skeleton with named regions (`header` · `body` · `badges` · `handles`) plus a stable `AtomNodeData` contract (optional `computeStatus?`, `lod?`). EPIC-260072 owns `body` (LOD/Blender block); EPIC-260069 writes `badges`. This epic ships the skeleton + contract only (T7) — no feature logic.
- [x] **Shared widget home** → `ui-primitives/` (ADR-260051), built as **presentational** components (data + callbacks injected; data hooks stay in api-contract / feature wiring) so all three consumers (search, navigator, COLLECT) reuse them.
- [x] **Demote `SearchResultPanel`** → removed from the left rail (left rail = navigators only); the graph re-scopes on search and represents the working set; the flat list returns as the Table view (EPIC-260071). Accept the brief graph-only interim until EPIC-260071 ships.
- [x] **Reference slice** → trivial Labels navigator + the existing Flow/Network views + a Details inspector tab that wraps today's `DetailPanel` as-is (EPIC-260067 later adds the Classify tab and removes the comma-box). Proves all three registries end-to-end with no new feature logic.
- [x] **Staging** → staged across the tasks below; the existing E2E suite stays green on every task (the regression gate).
- [x] **Coverage** → a dedicated full code + test coverage task is included below (mandatory per `epic-process.md`).

## Tasks

Inline checklist — no per-task files. Each task keeps the existing E2E suite green. Task state: `To Do → In Review → Done`.

| Task | State | Commit/PR/Review |
| --- | --- | --- |
| T1 · Write the ADR for the shell architecture (typed slot registries + `WorkspaceContext` + `AtomNodeData` node seam; extends ADR-260032, and cites/reconciles ADR-260051 for the `ui-primitives` widget home) — the approved decision record | Done | adr/ADR-260061.md |
| T2 · Introduce typed slot registries (navigators / views / inspector tabs) in `ui-shell`; wire the existing Flow/Network views + `DetailPanel` through them (no behavior change) | Done | `src/ui-shell/slots/` + registry-driven `GraphViewTabs` (authoritative `enabled` gate; flag in the view registry) + WorkspaceShell/GraphLegend; arch+tab tests updated; typecheck/lint/937 unit/47 e2e green |
| T3 · Add shell-owned `WorkspaceContext` (selection + working set); migrate `selectedAtomId` from props to context; update the ADR-260032 architecture assertion | Done | `workspace-context.tsx` + `Inspector.tsx` (+test, incl. onClose→select(null)); shell provides context, Inspector consumes selection; arch/D1 tests + ADR-260061 reconciled; typecheck/lint/942 unit/47 e2e green |
| T4 · Add the preferences store (localStorage, aligned with `ThemeProvider`): keys `homeEmphasis` / `computeBadges` with defaults `compute` / `always` | Done | `use-preferences.ts` (+test) exposed on `WorkspaceContext`; defaults `compute`/`always`; literal semantics + sticky-default documented; arch test locks the Q3 default; additive (no consumption yet); typecheck/lint/950 unit/47 e2e green |
| T5 · Collapsible left rail (persisted) + navigator slot + the trivial Labels navigator; remove `SearchResultPanel` from the left rail | Done | `LeftRail.tsx` (collapsible, `leftRailCollapsed` pref) + `LabelsNavigator.tsx` + navigator registry; `NavigatorProps` settled as a scoping contract (`{atoms}`, not atom-picker; ADR-260061 noted); `SearchResultPanel` removed from the shell (results in graph until Table/EPIC-260071); search + access-gating e2e repointed; typecheck/lint/960 unit/47 e2e green |
| T6 · Tabbed right-rail inspector with the Details tab (wraps current `DetailPanel`); collapsible / appears on selection | Done | `Inspector.tsx` → registry-driven tab strip + ARIA tabpanel wrapping `DetailPanel`, with arrow-key tab nav (mirrors GraphViewTabs); collapsible via new `rightRailCollapsed` pref; appears on selection; DetailPanel landmark preserved (no nesting); typecheck/lint/968 unit/47 e2e green |
| T7 · Node-extension seam: layered `AtomNode` (header/body/badges/handles) + `AtomNodeData` contract (no behavior change) | Done | `AtomNode` → named regions (handles/badges/body); `AtomNodeData` exported (open contract; `computeStatus`→badges/EPIC-260069, `lod`→body/EPIC-260072); badges slot gated on `computeStatus` (empty today, no behavior change); `CircleAtomNode` deferred + region/contract phrasing reconciled in ADR-260061; typecheck/lint/972 unit/47 e2e green |
| T8 · Shared widget scaffolds in `ui-primitives` (filter builder, permission-aware category tree, label chip editor) as presentational shells + barrels | Done | new `ui-primitives/widgets/`: `LabelChipEditor` + `CategoryTree` (`canEditNode?` Q2 seam) + `FilterBuilder`; style-neutral (classNames); contracts `WorkspaceFilter`/`CategoryFacetFilter`/`CategoryTreeNode` defined; barrel + CATALOG; **directory-wide styles-only boundary test (ADR-260051)**; 12 widget unit + arch tests; typecheck/lint/1007 unit/47 e2e green |
| T9 · Full code + test coverage for this epic; all existing E2E green | Done | Every new **module** F1 introduced at **100% line+branch** (Inspector, LeftRail, LabelsNavigator, workspace-context, use-preferences, slots/*, GraphViewTabs, GraphLegend, AtomNode, the three `ui-primitives/widgets` + barrel/types) — verified per ADR-260060 (measured, new modules held to 100% by review). Added 3 defensive-edge specs (empty navigator/inspector registries, `useWorkspace` outside provider) + targeted branch/error-path tests; refactored `Inspector` to drop redundant optional-chaining (`activeId`, key-nav uses `activeTabId`) so its last defensive branch is genuinely reachable and covered, not asserted-away. **Two files carry documented exemptions (not new modules, not held to the 100% bar):** (1) `WorkspaceShell.tsx` (72%/62.5%) — the modified composition root; per the framework testing-ladder, integration roots are verified by E2E, not exhaustive unit mocks. Its epic-new wiring (registry render, `WorkspaceProvider`, LeftRail/Inspector mount, view switching) is covered by `WorkspaceShell.test.tsx` (18 unit) + 47 E2E; residual uncovered = pre-existing delete-confirm/overlay/reduced-canvas paths (E2E-validated) **plus two epic-new but provably-unreachable "≥1 view always enabled" guards** (source of the invariant: Flow is ungated in `slots/view-registry.ts`, so `enabledViews` is never empty — L30 `?? 'flow'`, L60 `?? enabledViews[0]!`; the only state that reaches them, empty `enabledViews`, crashes the L60 non-null assertion, so no honest test can hit them. If a later epic gates Flow, revisit this exemption). (2) `CircleAtomNode.tsx` (66%) — untouched by this epic (T7 seam scoped to `AtomNode`/Flow). typecheck/lint/**1021 unit**/**47 e2e** green |

## Links (durable records spawned)

- Requirements: extends REQ-FR-260020, REQ-FR-260021, REQ-FR-260034 — **no new REQ created** (confirmed at T1: the shell realizes the existing extensibility-seam / adapter-resilience / dual-view requirements rather than adding one)
- ADRs: ADR-260061 (Application Shell — Slot Registries, Workspace Context, and Node-Extension Seam; extends ADR-260032, reconciles ADR-260051) — Accepted 2026-06-30
- ICRs: n/a
- Contracts: n/a
- Learnings: no separate learning record — the reusable lessons (the slot-registry / `WorkspaceContext` / `AtomNodeData` seam patterns, and coverage-by-review with documented unreachable-branch exemptions) are captured in ADR-260061 and governed by ADR-260060; nothing left uncaptured at close.
